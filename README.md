# Savor restaurant agents

A lightweight restaurant operations dashboard with a dependency-free Node.js API.

## Run locally

Install Node.js 18 or newer, then run `npm start` and open http://localhost:3000.

The API supports dashboard state, agent status, recommendations, approvals, tasks, optimization settings, and profit scenarios. Data is stored in `data/db.json`; use a database and authentication before production deployment.
